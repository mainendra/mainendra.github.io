{
    "homepage": "https://mainendra.github.io",
    "scripts": {
        "nojekyll": "cp .nojekyll build/",
        "predeploy": "npm run build && npm run nojekyll",
        "deploy": "gh-pages -b master -d build",
        "start": "snowpack dev",
        "build": "snowpack build && npm run nojekyll",
        "test": "web-test-runner \"src/**/*.test.jsx\"",
        "format": "prettier --write \"src/**/*.{js,jsx}\"",
        "lint": "prettier --check \"src/**/*.{js,jsx}\""
    },
    "dependencies": {
        "gh-pages": "^3.1.0",
        "react": "^16.13.1",
        "react-dom": "^16.13.1",
        "react-router-dom": "^5.2.0"
    },
    "devDependencies": {
        "@snowpack/plugin-dotenv": "^2.0.4",
        "@snowpack/plugin-optimize": "^0.2.8",
        "@snowpack/plugin-react-refresh": "^2.3.5",
        "@snowpack/plugin-webpack": "^2.1.1",
        "@snowpack/web-test-runner-plugin": "^0.1.3",
        "@testing-library/react": "^11.0.0",
        "@web/test-runner": "^0.9.0",
        "chai": "^4.2.0",
        "prettier": "^2.0.5",
        "snowpack": "^2.16.0"
    }
}
